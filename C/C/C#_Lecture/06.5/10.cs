// •0~10억까지의 정수 N과 0~9까지의 정수 M이 주어졌을 때, N에서 M이 몇 번 나오는지 출력해보기

//이거는 띄어쓰기가 있을 때

    // int [] arr = Array.ConvertAll(Console.ReadLine().Split(), int.Parse);
    // int num = int.Parse(Console.ReadLine());
    // int count = 0;
    //
    // for (int i = 0; i < arr.Length; i++)
    // {
    //     
    //     if (arr[i] == num)
    //     {
    //         count++;
    //     }
    // }
    // Console.WriteLine(count);

//띄어쓰기가 없을 때

    
    //string s = Console.ReadLine();
    //char ch = char.Parse(s);
    
    //string s = Console.ReadLine();
    //char ch = Convert.ToChar(s);
    
    /*
    string str = Console.ReadLine();
    int count = 0;
    
    char ch = Console.ReadLine()[0]; // string의 첫 번째 배열은 char

    for (int i = 0; i < str.Length; i++)
    {
        if (str[i] == ch)
        {
            count++;
        }
    }
    Console.WriteLine(count);
    
    */



//223712741 2 입력을 했을 때, 입력받을 수 있는 방법 

    // string[] input = Console.ReadLine().Split(); // 공백 기준으로 나눔
    //
    // string str = input[0];           // "223712741"
    // char target = input[1][0];       // '2'
    //
    
    