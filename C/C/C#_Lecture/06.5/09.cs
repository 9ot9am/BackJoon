// •임의의 정수 10개 중에, 100 미만인 숫자만 더해서 출력해보기
// •ex)
// 2, 99,207, 33, 102,39, 17, 997,2039, 5
// -> 195

    //임의의 int가 0개 들어간 배열 입력받기
    //int [i] -> 가 < 100 인지 아닌지 판단하기
    //if문 활용
    //100 미만이면 sum에 더하기
    //sum에 출력
    
    // int [] arr = Array.ConvertAll(Console.ReadLine().Split(), int.Parse);
    // int sum = 0;
    //
    // for (int i = 0; i < arr.Length; i++)
    // {
    //     if (arr[i] < 100)
    //     {
    //         sum += arr[i];
    //     }
    // }
    // Console.WriteLine(sum);