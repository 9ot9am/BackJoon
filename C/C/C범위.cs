     // string str = Console.ReadLine();
     //
     // //문자열을 공백 기준으로 쪼갬
     // string [] numbers = str.Split(" ");
     //
     // //쪼갠 문자열들 중에 첫번째 것을 int형으로 변환
     // int a = int.Parse(numbers[0]);
     // //쪼갠 문자열들 중에 두번째 것을 int형으로 변환
     // int b = int.Parse(numbers[1]);
     //
     // // a = Math.Min(Math.Max(a, 1), 10000);  // A가 1~10,000 사이로 제한
     // // b = Math.Min(Math.Max(b, 1), 10000);
     //
     //
     //
     // //두 값을 더해서 출력
     // Console.WriteLine(a+b);
     // Console.WriteLine(a-b);
     // Console.WriteLine(a*b);
     // Console.WriteLine(a/b);
     // Console.WriteLine(a%b);
     //
     //
     //